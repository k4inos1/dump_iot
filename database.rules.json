{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "auditoria": {
      "live_tracking": {
        ".indexOn": ["last_seen", "rssi"]
      },
      "redes_detectadas": {
        ".indexOn": ["power", "last_seen"]
      },
      "reportes_vuln": {
        ".indexOn": ["timestamp"]
      }
    }
  }
}
